<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ListApp"
        parent="Theme.Material3.DayNight.NoActionBar">

        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="android:colorBackground">@color/colorPrimaryBackground</item>
        <item name="bottomAppBarStyle">@color/colorPrimaryBars</item>
        <item name="toolbarStyle">@color/colorPrimaryBars</item>

    </style>

    <style name="Theme.ListApp" parent="Base.Theme.ListApp" />
</resources>